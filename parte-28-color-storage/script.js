console.log(localStorage.nome = 'Silva')

function saveValue(nome, value) {
  localStorage[nome] = value

  
}

function setValues() {
  const properties = Object.keys(localStorage)

  properties.forEach(propertie => {
  
  })
}